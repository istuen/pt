// src/compile/index.ts — Compile 中端入口（Phase 7.1 占位）
//
// Phase 7.1: 目录骨架建立。Phase 7.5 将实现 compileContext(blueprint, channel, domains) → Context IR。
//
// Phase 5.5 状态：midend/ 已退出（layout 逻辑并入 src/render/prompt.ts）。
// Phase 7 状态：恢复中端为 compile/，职责是把 (Blueprint + Channel + Domains) 编译成 Context IR。
export {};