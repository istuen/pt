// src/compile/index.ts — Compile 中端入口
//
// Phase 9.4：v9 中端导出——compileContext(profile, blueprint, domains) → Context IR。
export { compileContext, computeSourceHash, registerModuleRenderer } from "./context.js";
