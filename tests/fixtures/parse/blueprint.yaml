name: test-blueprint
groups:
  - name: 会话背景
    inject: session
    mode: hybrid
    modules: [Scene, Participant]
  - name: 触发索引
    inject: session
    modules: [Trigger]
  - name: 参考手册
    inject: turn
    modules: [Rules, Flows, Checklists]
